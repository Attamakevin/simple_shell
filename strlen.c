#include "shell.h"

/**
 * _strlen - function returns the length of a string
 * @c: inputted string
 * Return: len( The length of string)
 */
int _strlen(char *c)
{
	int len = 0;

	while (*(c + i))
	{
		i++;
	}
	return (len);
}
